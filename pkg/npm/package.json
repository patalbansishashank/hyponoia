{
  "name": "hyponoia",
  "version": "0.8.1",
  "description": "Code context engine for AI coding agents — indexes repositories into a queryable graph and returns exact code spans instead of whole files",
  "mcpName": "io.github.patalbansishashank/hyponoia",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/patalbansishashank/hyponoia.git"
  },
  "homepage": "https://github.com/patalbansishashank/hyponoia",
  "bugs": {
    "url": "https://github.com/patalbansishashank/hyponoia/issues"
  },
  "keywords": [
    "mcp",
    "claude",
    "code-intelligence",
    "codebase",
    "memory",
    "ai",
    "llm",
    "tree-sitter"
  ],
  "bin": {
    "hyponoia": "./bin.js"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "postinstall": "node install.js"
  },
  "engines": {
    "node": ">=18"
  },
  "_comment_os_cpu": "RETIRED-PLATFORM(macos): darwin dropped — no macOS asset is published. RETIRED-PLATFORM(windows-arm64): arm64 stays for linux-arm64; npm's os/cpu are independent arrays and cannot express 'win32 only on x64', so install.js falls back to the x64 build under emulation at runtime. See docs/MAINTAINERS.md 'Retired platforms'.",
  "os": ["linux", "win32"],
  "cpu": ["x64", "arm64"],
  "files": [
    "bin.js",
    "install.js",
    "README.md"
  ]
}
