# Tracked non-source files that are NOT build artifacts.
#
# Read by scripts/ci/check-tracked-artifacts.sh (NEXT-STEPS.md §3.2 step 6), the
# gate that exists because a 9,428,133-byte compiled Go binary was tracked from
# dd41cf92 and shipped inside the v0.3.0 and v0.3.1 tags.
#
# Format: <glob> : <why this file is not a build artifact>
#   - the justification is MANDATORY; a line without `:` is an error
#   - `*` and `?` do NOT cross `/`; write `**/` to span directories
#   - an entry that matches no tracked file is an ERROR, not a warning. A stale
#     exemption is how an allowlist quietly becomes a hole.
#
# Adding a line here is a decision, not a formality: it says this file is
# checked in on purpose, will be reviewed on purpose, and will ship inside
# every tag cut from this tree.

# ── Embedding vectors: INPUT to the product, not output of a compiler ────────
# 31 MB each. Extracted once from the model weights by
# scripts/extract_nomic_vectors.py and pinned, so the retrieval numbers in
# NEXT-STEPS §2 are reproducible from the tree alone; regenerating them needs
# the model on the machine, which CI does not have. Verified by
# scripts/verify-vector-artifacts.py.
vendored/*/code_vectors.bin : pinned embedding vectors — reproducibility input, not a compiler output

# ── Documentation image ──────────────────────────────────────────────────────
# The graph UI screenshot docs/GRAPH.md renders. A PNG is binary by nature and
# there is no smaller honest form of a screenshot.
docs/graph-ui-screenshot.png : documentation screenshot referenced by docs/GRAPH.md
